class Calculations
{
private:
	double numA;
	double numB;
	double result;
public:
	Calculations(double, double);
	Calculations();
	void Addition();
	double getNumA();
	double getNumB();
	double getResult();
	void setNumA(double);
	void setNumB(double);
//protected:// we can also use protected but in this case we dont
};

